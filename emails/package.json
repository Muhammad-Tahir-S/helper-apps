{
  "name": "emails",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "email dev --dir ./templates --port 3001",
    "build": "email build --dir ./templates --packageManager pnpm",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "@react-email/preview-server": "4.3.2",
    "react-email": "^4.3.2"
  },
  "dependencies": {
    "@react-email/components": "0.5.7"
  }
}
